apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: wardrobe
  labels:
    app: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      automountServiceAccountToken: false
      containers:
        - name: frontend
          image: wardrobe/frontend:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          securityContext:
            runAsNonRoot: true
            runAsUser: 1001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            seccompProfile:
              type: RuntimeDefault
            capabilities:
              drop:
                - ALL
          env:
            - name: NEXTAUTH_URL
              valueFrom:
                configMapKeyRef:
                  name: wardrobe-config
                  key: NEXTAUTH_URL
            - name: NEXTAUTH_SECRET
              valueFrom:
                secretKeyRef:
                  name: wardrobe-secrets
                  key: nextauth-secret
            - name: BACKEND_URL
              value: "http://backend:8000"
            - name: OIDC_ISSUER_URL
              valueFrom:
                configMapKeyRef:
                  name: wardrobe-config
                  key: OIDC_ISSUER_URL
            - name: OIDC_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: wardrobe-secrets
                  key: oidc-client-id
            - name: OIDC_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: wardrobe-secrets
                  key: oidc-client-secret
            - name: OIDC_END_SESSION_URL
              valueFrom:
                configMapKeyRef:
                  name: wardrobe-config
                  key: OIDC_END_SESSION_URL
                  optional: true
            - name: NODE_TLS_REJECT_UNAUTHORIZED
              value: "0"
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "300m"
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: wardrobe
spec:
  selector:
    app: frontend
  ports:
    - port: 3000
      targetPort: 3000
